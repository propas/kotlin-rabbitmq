spring:
  profiles:
    active: picture-topic-exchange
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    #listener:
     # simple:
      #  acknowledge-mode: manual